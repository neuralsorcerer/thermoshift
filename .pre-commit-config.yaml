repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: Ruff lint and import checks
        entry: python -m ruff check
        language: system
        types_or: [python, pyi, jupyter]

      - id: ruff-format
        name: Ruff formatting check
        entry: python -m ruff format --check
        language: system
        types_or: [python, pyi, jupyter]
